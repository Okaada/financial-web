## 1. AccountForm

- [x] 1.1 `AccountForm.tsx`: quando `kind === 'investment'`, não renderizar o campo "Saldo inicial"; enviar `openingBalance: 0` automaticamente; pular validação de `parseToCentsSigned` para esse tipo.

## 2. Verificação

- [x] 2.1 `npm run typecheck && npm run lint && npm run build` — limpos
