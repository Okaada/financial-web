# SPA fallback: serve index.html for client-side navigation routes that aren't a
# static asset. `/api/*` never reaches Pages — the router Worker's route takes
# precedence over Pages for that path (see same-origin-routing spec).
/*    /index.html    200
